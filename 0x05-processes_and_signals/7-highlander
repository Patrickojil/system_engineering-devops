#!/usr/bin/env bash
# This script sends SIGTERM to the 7-highlander process

# Find all instances of 7-highlander
pids=$(pgrep -f "7-highlander")

# Send SIGTERM to each one
for pid in $pids; do
    kill -TERM "$pid" 2>/dev/null
    echo "Sent SIGTERM to 7-highlander (PID: $pid)"
done
