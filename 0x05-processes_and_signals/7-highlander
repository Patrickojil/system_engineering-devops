#!/usr/bin/env bash
# This script sends SIGTERM to the 7-highlander process

# Find the PID of 7-highlander
pid=$(pgrep -f "7-highlander")

# Send SIGTERM signal if found
if [ -n "$pid" ]; then
    # Use Bash's built-in kill to send SIGTERM (signal 15)
    kill -TERM "$pid" 2>/dev/null
fi
