#!/usr/bin/env bash
# This script displays "To infinity and beyond" indefinitely and handles SIGTERM

# Trap SIGTERM signal (15) to print message instead of terminating
trap 'echo "I am invincible!!!"' TERM

# Infinite loop
while true; do
    echo "To infinity and beyond"
    sleep 2
done
